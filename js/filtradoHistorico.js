const button_filter = document.getElementById('filter_button');
        
if (button_filter) {  // Asegurarse de que el botón existe
    button_filter.addEventListener('click', function () {
        alert('hello');
    });
}